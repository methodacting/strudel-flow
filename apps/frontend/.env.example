# Frontend Environment Variables

```
# For local development only (put this in .env.local)
VITE_BACKEND_URL=https://backend.strudel-flow.localhost
```

For production, leave unset to use same-origin.
